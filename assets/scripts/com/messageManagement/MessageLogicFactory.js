let DataKey = require('DataKey');

cc.Class({
    getMessageHandlerList: function () {
        let handlers = [];

        return handlers;
    },

    getMessageFilterList: function (handlers) {
        let result = [];

        return result;
    },
});